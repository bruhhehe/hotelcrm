# Decisions

What was chosen, why, and when. Newest phase at the bottom. If you're about to change one of
these, update this file in the same commit.

## Phase 1 — Scaffold

| #    | Decision                                                                                                                                                                             | Why                                                                                                                                                                                                                                                                                                        |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1.1  | **Next.js 15.5 + React 19**, App Router, `typedRoutes: true`                                                                                                                         | Spec. Typed routes catch broken internal links at build time.                                                                                                                                                                                                                                              |
| 1.2  | **TypeScript strict + `noUncheckedIndexedAccess`**                                                                                                                                   | Array/record lookups in the pricing engine (nightly prices, rate lookups) must handle "missing" explicitly. ESLint bans `any`.                                                                                                                                                                             |
| 1.3  | **Zod 4**                                                                                                                                                                            | Current major. `lib/env/schema.ts` is pure and unit-tested; `lib/env/server.ts` (server-only) and `lib/env/client.ts` wrap it.                                                                                                                                                                             |
| 1.4  | **All integration keys are optional**, validated for shape when present; paired keys (Google id+secret, Resend key+from, Twilio trio, Calendar id+secret) must be set together       | Spec §8: "not configured" is a UI state, not a crash. A malformed key still fails boot with every bad key listed. Empty strings count as unset (Vercel commonly leaves `""`).                                                                                                                              |
| 1.5  | Google Places key is **`NEXT_PUBLIC_GOOGLE_PLACES_KEY`**, not `GOOGLE_PLACES_KEY`                                                                                                    | It's used client-side (spec §2), and Next.js only exposes `NEXT_PUBLIC_*` to the browser. Restrict it by HTTP referrer in Google Cloud.                                                                                                                                                                    |
| 1.6  | **node-postgres (`pg`) driver** via `drizzle-orm/node-postgres`, not `@neondatabase/serverless` over HTTP                                                                            | The availability engine needs interactive transactions with `SELECT … FOR UPDATE` (spec §6.1); the Neon HTTP driver can't do that. `pg` works the same against Neon (use the pooled `-pooler` URL) and against local/CI Postgres, so tests run on a real database. Pool is cached on `globalThis` (max 5). |
| 1.7  | DB access via **`getDb()`**, which throws `DatabaseNotConfiguredError` when `DATABASE_URL` is unset                                                                                  | Nothing touches the DB at import time, so `next build` and the marketing site work before Neon is connected.                                                                                                                                                                                               |
| 1.8  | **Auth.js v5 (`next-auth@5.0.0-beta.32`)** with the Drizzle adapter, **database sessions**                                                                                           | Sessions can be revoked (remove a staff member → they're signed out), and memberships/roles are looked up server-side anyway. Falls back to JWT only when there's no DB, in which case there are no providers and nobody can sign in.                                                                      |
| 1.9  | Auth config is **lazy** (`NextAuth(() => config)`)                                                                                                                                   | Providers are registered only when configured. Magic link needs DB + (Resend, or non-production). Google needs DB + id/secret.                                                                                                                                                                             |
| 1.10 | **Dev magic links print to the console** when Resend isn't configured; production never does this                                                                                    | Spec §7 ("magic link printed to console in dev"). The guard is `NODE_ENV !== "production"`.                                                                                                                                                                                                                |
| 1.11 | Magic-link sign-in calls `signIn(…, { redirect: false })` then `redirect("/login/check-email")`                                                                                      | Otherwise Next.js follows Auth.js's `/api/auth/verify-request` hop as a soft navigation and the address bar shows the API URL.                                                                                                                                                                             |
| 1.12 | **Middleware only checks that a session cookie exists** (no DB, edge-safe); `(app)/layout.tsx` does the real check via `requireUser()`                                               | This is the Next.js pattern that keeps the DB out of middleware. A forged or expired cookie still gets redirected by the layout.                                                                                                                                                                           |
| 1.13 | Post-sign-in `callbackUrl` must be a same-origin path (`safeCallbackUrl`)                                                                                                            | Blocks open redirects: `//evil.com`, `/\evil.com`, absolute URLs.                                                                                                                                                                                                                                          |
| 1.14 | Staff auth tables are `users/accounts/sessions/verification_tokens`. **Guest-portal auth is a separate table + cookie** (Phase 9)                                                    | Spec §2. A guest must never be able to obtain a staff session.                                                                                                                                                                                                                                             |
| 1.15 | IDs are **cuid2** generated in the app (`lib/db/schema/_shared.ts`)                                                                                                                  | Spec §3. The id is known before insert, which is handy for idempotent writes and audit rows.                                                                                                                                                                                                               |
| 1.16 | **Migrations run before the build on Vercel** (`vercel-build` = `db:migrate && next build`), and are skipped when `DATABASE_URL` is unset                                            | With the Neon ↔ Vercel integration, each preview deploy gets its own DB branch, so previews migrate their branch and production migrates at promote time. `db:migrate` uses Drizzle's programmatic migrator, which is idempotent.                                                                          |
| 1.17 | Vercel region **`lhr1` (London)**                                                                                                                                                    | Demo tenant and primary market are UK. Pair it with a Neon project in `aws-eu-west-2`.                                                                                                                                                                                                                     |
| 1.18 | **Security headers**: `nosniff`, `Referrer-Policy`, `Permissions-Policy` everywhere; `X-Frame-Options: DENY` everywhere **except** `/stay/[slug]`                                    | The booking widget must embed in an iframe on hotels' own sites; nothing else should be frameable, including the guest portal (`/stay/[slug]/account`).                                                                                                                                                    |
| 1.19 | **UI primitives written by hand in shadcn style** (`components/ui/*`, Radix + CVA), not via the shadcn CLI                                                                           | The CLI is interactive, and the spec says to re-theme anyway. Tokens live in `app/globals.css` (`:root` vars → Tailwind v4 `@theme inline`).                                                                                                                                                               |
| 1.20 | Fonts: **Fraunces** (display, `font-display`) + **Inter** (body) via `next/font`                                                                                                     | Spec §9. Self-hosted at build time, so there's no runtime request to Google.                                                                                                                                                                                                                               |
| 1.21 | Sections not built yet show an **honest "Not set up yet" empty state**, and quick-create items are disabled ("Soon")                                                                 | "Never fake a feature." Each is replaced in its phase.                                                                                                                                                                                                                                                     |
| 1.22 | Dates are formatted in the **hotel's timezone** (`greetingFor`, `longDate`), with `Europe/London` as the default until hotels exist                                                  | The server runs in UTC. A London hotel at 00:30 BST must still see "today" as today.                                                                                                                                                                                                                       |
| 1.23 | CI = typecheck, lint, Prettier check, Vitest, `drizzle-kit check`, **schema-drift check** (generate must be a no-op), migrations applied to a real Postgres 16 service, `next build` | Catches "changed the schema but forgot the migration" before merge.                                                                                                                                                                                                                                        |
| 1.24 | Vitest config is `vitest.config.mts` with native `resolve.tsconfigPaths`                                                                                                             | Vite 7+ supports tsconfig paths natively, so `vite-tsconfig-paths` isn't needed.                                                                                                                                                                                                                           |
| 1.25 | Server code reads the session through **`getSession()`**, which returns `null` without calling Auth.js unless both DB and `AUTH_SECRET` are set                                      | Keeps an unconfigured deploy quiet instead of logging `MissingSecret` on every request. Sign-in needs `AUTH_SECRET` in dev too, because Auth.js requires it everywhere.                                                                                                                                    |

### Open questions (defaults applied, not blocking)

- **Stripe Connect fee**: assumed Lodgely takes no application fee on guest payments in v1. Revisit before Phase 7.
- **Neon/Vercel linking**: this session has no access to Vercel. Linking the project and adding the Neon integration is a manual step (see README).

## Tooling

| #   | Decision                                                                                                                                                                                                                                                | Why                                                                                                                                                                                      |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T.1 | **Impeccable** design skill (github.com/pbakaus/impeccable, engine v0.1.5) is vendored at project scope: `.claude/skills/impeccable/` plus 4 subagents in `.claude/agents/`, installed with `npx impeccable install --providers=claude --scope=project` | Committing it means every Claude Code session gets `/impeccable <command>` (audit, critique, polish, harden…), including ephemeral cloud sessions. Refresh with `npx impeccable update`. |
| T.2 | The ~16 MB **engine binary is gitignored**; the launcher downloads the pinned, checksum-verified build into `~/.impeccable/bin` on first use                                                                                                            | A binary built for one platform is useless on the others and would bloat the repo.                                                                                                       |
| T.3 | The design **hook stays in `.claude/settings.local.json`** (gitignored, per machine), where the installer puts it, rather than in the shared `settings.json`                                                                                            | A shared hook would run a downloaded binary after every Edit/Write in every session. Whether to opt the whole repo in is left to the owner.                                              |
| T.4 | `.claude/` is excluded from Prettier and ESLint                                                                                                                                                                                                         | It's vendored third-party content managed by the Impeccable updater, not our code.                                                                                                       |

## Design audit (Impeccable, 2026-09-24)

| #    | Decision                                                                                                                                   | Why                                                                                                                                                                                                                                        |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| D.1  | `--input` darkened to `#8f8676` (form-control borders); `--border` stays light for decorative edges                                        | Input boundaries were 1.45:1; WCAG 1.4.11 needs 3:1. Now 3.39–3.60:1 on every surface.                                                                                                                                                     |
| D.2  | **One global focus indicator**: `:focus-visible { outline: 2px solid var(--ring); outline-offset: 2px }`, with ring colour set at rest     | The old ring was green at 40% alpha (1.8:1) and links had a 1px half-alpha outline (2.1:1). A solid ring is 4.5–5.4:1. Setting the colour at rest stops `transition-colors` animating it in from `currentColor`.                           |
| D.3  | Buttons and inputs use **`min-h-*` + vertical padding** instead of fixed `h-*`; interactive controls grow to **44px on `pointer: coarse`** | Text zoom no longer clips labels (and clears the detector's `cramped-padding`); touch targets meet 44px without bloating the mouse UI.                                                                                                     |
| D.4  | Every route has one `<main>` and one `<h1>`; the app shell has a **skip link** to `#main`                                                  | axe `landmark-one-main` / `page-has-heading-one` on auth, widget, portal and 404; WCAG 2.4.1.                                                                                                                                              |
| D.5  | ⌘K palette is an **ARIA combobox** (`aria-activedescendant`, listbox/option without nested buttons, live result count)                     | Screen readers previously heard nothing when arrowing through results.                                                                                                                                                                     |
| D.6  | No decorative blur (top bar, modal scrim); scrim and card elevation are tokens (`bg-overlay`, `shadow-card`)                               | Blur-as-decoration is on the craft floor's refuse list and repaints on every scroll frame; raw hex in components broke the token rule.                                                                                                     |
| D.7  | **No kicker/eyebrow above headings.** The dashboard date moved under the greeting as a `<time>`, and the marketing eyebrow is gone         | A craft-floor ban no brief earns back. The spec's "greeting + date" is still met. The sidebar's "NAVIGATE" label stays: it labels a nav list, not a heading, and the spec pins it.                                                         |
| D.8  | `EmptyState`: left-aligned copy with an inline icon, not an icon tile stacked above the heading; takes `headingLevel`                      | The tile-above-heading is a generic template tell, and pages whose empty state _is_ the content need an h1.                                                                                                                                |
| D.9  | Marketing copy only states facts the spec backs ("30-day free trial", "no card"); removed "Set up in an afternoon" and "Cancel anytime"    | Those were unverified claims written in Phase 1.                                                                                                                                                                                           |
| D.10 | Detector exceptions live in `.impeccable/config.json`: `overused-font` for Inter and Fraunces, `cream-palette` scoped to `app/globals.css` | Spec §9 pins the cream `#FAF8F3` and the serif/sans pairing (and Geist, the other option, is also on the overused list). The brief wins. Rendered-URL scans still report `cream-palette`, because file-scoped ignores don't apply to URLs. |
| D.11 | `getSession()` is wrapped in React `cache()`                                                                                               | Layout and page both call `requireUser()`; each app request hit the sessions table twice.                                                                                                                                                  |
| D.12 | The sign-in email is **normalised before validation** (`lib/auth/sign-in-schema.ts`), and the form echoes the input back on error          | Zod 4's `z.email().trim()` validates before trimming, so pasted addresses with spaces were rejected. React 19 resets fields after an action, which wiped the user's input.                                                                 |

## Design direction v2 (owner-requested redesign, 2026-09-24)

The owner asked to "make it not look AI". The Phase 1 look (warm cream, Fraunces serif display, Inter, rounded-2xl cards, tracked-caps micro-labels) is the most common AI-generated interface look, and the spec pinned it. The owner chose a new visual world, then picked **conventional SaaS at the craft level of the Airbnb host app** over the rolled direction (a hotel key-rack world). Spec §9 was rewritten to match. DESIGN.md records the result.

| #    | Decision                                                                                                                                                                                                                                                | Why                                                                                                                                                           |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| V.1  | **Supersedes 1.20, 1.19 (token values), D.7's dashboard kicker note, D.8 and D.10.** White ground `#FFFFFF`, ink `#222222`, secondary `#6A6A6A`, fills `#F7F7F7`, hairlines `#EBEBEB`, input borders `#8E8E8E`, one accent `#0B7A55`, focus ring `#222` | The benchmark's palette, with every pair checked: the accent is 5.3:1 with white, secondary text 5.4:1, input borders ≥3:1 (D.1 still holds, at a new value). |
| V.2  | **Figtree** for everything, bold/extrabold sentence-case headings; Fraunces, Inter, `font-display` and the `micro-label` utility removed                                                                                                                | One friendly geometric-humanist face in the spirit of Airbnb's Cereal. It's not on the detector's overused list; Fraunces, Inter and Geist all are.           |
| V.3  | Detector exceptions for cream, Inter and Fraunces **removed** from `.impeccable/config.json`                                                                                                                                                            | They no longer apply. Keeping them would hide a regression back to the old look.                                                                              |
| V.4  | Phones get a **bottom tab bar** (Dashboard, Calendar, Reservations, Guests, Menu); Menu is a bottom sheet with every section in §4 order plus account actions. The hamburger drawer is gone                                                             | The host-app pattern: primary sections one thumb-tap away for an owner working from a phone.                                                                  |
| V.5  | Resting cards use a hairline, not a shadow; soft elevation (`shadow-float`, `shadow-pill`) only on floating layers                                                                                                                                      | Keeps the page flat and calm, with depth where something floats.                                                                                              |
| V.6  | **One treatment for primary actions**: green, 8px radius. The `dark` button is a strong secondary only                                                                                                                                                  | The reviewer caught a black pill and a green rectangle both used as "Start free trial".                                                                       |
| V.7  | No `Sparkles` glyph: Housekeeping uses a cleaning brush, What's new a megaphone                                                                                                                                                                         | Two destinations shared one icon, and the sparkle is the recognised "AI" glyph.                                                                               |
| V.8  | The dashboard greeting has its own hero scale (34/44px extrabold); section titles stay at 28/32px                                                                                                                                                       | The greeting is the app's opening moment and should outrank section titles.                                                                                   |
| V.9  | Guest-facing pages (widget, portal) sit in a `GuestFrame` with a quiet "Powered by Lodgely" credit                                                                                                                                                      | A guest should see who is speaking. The page belongs to the hotel, so Lodgely stays a credit.                                                                 |
| V.10 | Spec §4.1's "4 cards with coloured left rule" is replaced by §9's soft tinted states                                                                                                                                                                    | A coloured border-left above 1px is on the craft floor's refuse list, and the owner retired the old look.                                                     |

## Phase 2: Schema and seed

| #    | Decision                                                                                                                                                                                                                                                                                         | Why                                                                                                                                                                                      |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2.1  | **Money is integer minor units; percentages are integer basis points** (2000 = 20%). All rounding goes through `lib/money/math.ts` (`divRound`, half away from zero, integer-only)                                                                                                               | No floating-point pennies. VAT back-calculation, deposits and discounts round the same way everywhere.                                                                                   |
| 2.2  | **Hotel calendar dates are Postgres `date` read as `"YYYY-MM-DD"` strings**; instants are `timestamptz`. `lib/dates` does calendar maths and hotel-local → UTC conversion                                                                                                                        | A stay night is a calendar date, not an instant, so it can't slip a day across timezones or DST.                                                                                         |
| 2.3  | **Every tenant table has `hotel_id` + `unique (hotel_id, id)`, and child → parent references are composite `(hotel_id, x_id)` foreign keys**                                                                                                                                                     | The database itself rejects a row in hotel A pointing at hotel B's data, whatever the application code does. Tested.                                                                     |
| 2.4  | **`db.forHotel(hotelId)`** (`lib/db/tenant.ts`) scopes reads (plus soft-delete filtering), stamps `hotel_id` on inserts and confines updates and deletes. ESLint forbids importing `getDb` outside `lib/db`, `lib/auth` and `lib/hotels`; a schema test fails if a tenant table lacks `hotel_id` | "No query can forget it" (spec §2), enforced by the linter, a test and the FKs rather than by convention.                                                                                |
| 2.5  | **No double bookings at the database level**: `reservation_rooms` carries the stay dates, plus a `btree_gist` exclusion constraint on `(hotel_id, room_id, daterange(check_in, check_out, '[)'))` for active, assigned rows (migration 0002)                                                     | A backstop under the Phase 3 locking. Half-open ranges mean checking out on day D frees night D. Cancelled and no-show stays set `is_active = false`.                                    |
| 2.6  | **Postgres enums** for the closed sets the spec defines                                                                                                                                                                                                                                          | Invalid values are impossible. Adding a value is a one-line migration; only stable sets are enums.                                                                                       |
| 2.7  | `reservations.nights` is a **stored generated column** (`check_out - check_in`)                                                                                                                                                                                                                  | Spec says "nights (computed)", and it can never disagree with the dates.                                                                                                                 |
| 2.8  | **Invoice numbers are assigned only at issue** (drafts have none; a check constraint enforces it) from `hotel_counters`, via an upsert in the same transaction (`lib/db/counters.ts`)                                                                                                            | Gap-free per hotel (§6.3): a rolled-back transaction gives its number back. Tested, including concurrency. Reservation references (`LDG-000123`) use the same counter mechanism.         |
| 2.9  | Payments: refunds are negative (`kind = refund`, a check enforces the sign); a payment belongs to a reservation and is linked to an invoice once issued; package redemptions must reference a `guest_packages` row                                                                               | §6.3, enforced in the schema rather than only in code.                                                                                                                                   |
| 2.10 | Tables added beyond the §3 list: `invites` (invite flow), `hotel_counters`, `guest_packages` (what a guest bought, redeemed at check-in); columns `hotels.stripe_connect_account_id`, `guests.nationality` (analytics), `guests.lapsed_at` (inactivity automation)                               | Each is required by a spec screen or rule; nothing speculative. Guest-portal auth tables wait for Phase 9.                                                                               |
| 2.11 | Soft delete (`deleted_at`) on user-managed records (room types, rooms, rate plans, extras, promo codes, guests, reservations, automations…); append-only logs have none. Deleting a hotel hard-deletes everything by cascade; child → child FKs use `no action` so that cascade completes        | Users can restore mistakes; closing an account removes all of its data.                                                                                                                  |
| 2.12 | Seed: "3 rate plans" means **three plans on every room type** (15 rows); guest emails on reserved `example.*` domains; UK phones in Ofcom's drama range; the seed only deletes the `fell-view` hotel and `@fellview.demo` users, and refuses production without `ALLOW_SEED=1`                   | Seeded data can never reach a real person, and re-seeding can't touch real hotels.                                                                                                       |
| 2.13 | The seed is a **pure, deterministic generator** (`scripts/seed/generate.ts`) plus a writer. Invariants are unit-tested on three different "today"s: no overlapping stays, money consistent with status, gap-free invoices, nothing dated in the future                                           | A seed that silently breaks the dashboard's numbers is worse than none.                                                                                                                  |
| 2.14 | Scripts can reach Neon over **HTTPS** (`--https`, the Neon serverless driver) as well as Postgres TCP; migrations prefer `DATABASE_URL_UNPOOLED`                                                                                                                                                 | Neon recommends a direct connection for DDL. HTTPS works where outbound 5432 is blocked, as in this sandbox.                                                                             |
| 2.15 | **Vercel region `iad1`** (supersedes 1.17's `lhr1`)                                                                                                                                                                                                                                              | The owner's Neon database is in AWS `us-east-1`. Functions should sit next to the database, since each request makes several round trips. Revisit if the database moves to an EU region. |
| 2.16 | Database tests only use an explicit **`TEST_DATABASE_URL`**, never `DATABASE_URL`                                                                                                                                                                                                                | `DATABASE_URL` may point at the real Neon database; tests must never write there.                                                                                                        |
| 2.17 | The current hotel is the user's first membership, or the one named in the `lodgely_hotel` cookie if they belong to it (`lib/hotels/current.ts`, memoised per request)                                                                                                                            | Multi-property switching (Scale) can set the cookie later without changing callers.                                                                                                      |
